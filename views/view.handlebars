<div id="faq-view-container" class="container">
    <div class="row text-center">
        <a class="back-link main-color" href="/">Voltar</a>
        <div id="img-container" class="col-md-12">
            <img src="/img/servico.svg" alt="Serviço">
        </div>
        <div class="col-md-12">
            <h1 class="main-color">{{faq.dataValues.titulo}}</h1>
            <p class="bold">Descrição do erro:</p>
            <p class="faq-desc">{{faq.dataValues.descricao}}</p>
            <p><span class="bold">Solução encontrada: </span><br>{{faq.dataValues.solucao}}</span></p>
            <p>
                Caso encontre um solução alternativa, envie um e-mail para <a href="mail:to{{faq.dataValues.email}}">{{faq.dataValues.email}}</a>
            </p>
        </div>
    </div>
</div>